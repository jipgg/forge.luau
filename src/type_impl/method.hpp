#ifndef TYPE_METHOD_HPP
#define TYPE_METHOD_HPP

enum class method {
    read_all,
    children,
    string,
    generic_string,
    extension,
    has_extension,
    replace_extension,
    has_stem,
    stem,
    filename,
    has_filename,
    replace_filename,
    remove_filename,
    parent_path,
    has_parent_path,
    relative_path,
    has_relative_path,
    is_absolute,
    is_relative,
    root_directory,
    has_root_directory,
    lexically_normal,
    lexically_proximate,
    lexically_relative,
    print,
    write,
    writeln,
    flush,
    is_open,
    scope,
    line_iterator,
    eof,
    close,
    close_after,
    write_string,
    absolute,
    is_directory,
    is_file,
    is_symlink,
    directory_iterator,
    println,
    ln,
    endl,
    parent,
    child,
    descendants,
    each_child,
    each_descendant,
    remove_extension,
    COMPILE_TIME_ENUM_SENTINEL
};
#endif
